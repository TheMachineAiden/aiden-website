const labels=document.querySelectorAll('.label-button');labels.forEach(button=>button.addEventListener('click',()=>{const note=document.getElementById(button.getAttribute('aria-controls'));const isOpen=button.getAttribute('aria-expanded')==='true';button.setAttribute('aria-expanded',String(!isOpen));button.querySelector('span').textContent=isOpen?'+':'−';button.childNodes[0].textContent=isOpen?'Read the label ':'Close the label ';note.hidden=isOpen;}));const lamp=document.querySelector('.lamp');lamp.addEventListener('click',()=>{const lit=lamp.getAttribute('aria-pressed')==='true';lamp.setAttribute('aria-pressed',String(!lit));document.body.classList.toggle('lights-on',!lit);lamp.lastChild.textContent=lit?' House lights':' Dim lights';});document.querySelector('.archive-form').addEventListener('submit',event=>{event.preventDefault();const input=event.currentTarget.elements.almost,status=event.currentTarget.querySelector('.form-status');if(!input.value.trim()){status.textContent='The archive needs a fragment first.';input.focus();return;}status.textContent='Filed for safekeeping. It can stay unfinished here.';input.value='';});document.querySelector('#year').textContent=`© ${new Date().getFullYear()}`;
